# Server-rendered pages call the separate backend directly.
INTERNAL_API_URL=http://localhost:4000/api
BACKEND_API_URL=http://localhost:4000/api

# Browser actions use the frontend server to reach the backend. No backend URL
# is included in the browser bundle.

# Site URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
